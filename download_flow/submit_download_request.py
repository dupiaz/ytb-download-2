from typing import Dict


def submit_download_request(url: str) -> Dict[str, str]:
    """Gửi yêu cầu tải video từ UI đến ứng dụng/service."""
    # TODO: Gọi API hoặc logic nội bộ để xử lý yêu cầu tải xuống
    return {"status": "pending", "url": url}
