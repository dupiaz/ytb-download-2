from typing import Dict


def receive_video_data(video_data: Dict[str, str]) -> Dict[str, str]:
    """Nhận dữ liệu video đã tải về từ YouTube."""
    # TODO: Xử lý dữ liệu video nếu cần trước khi lưu tạm
    return video_data
